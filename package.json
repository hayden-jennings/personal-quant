{
  "private": true,
  "name": "personal-quant",
  "version": "0.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "yarn --cwd apps/web dev",
    "dev:api": "yarn --cwd apps/api dev"
  }
}
